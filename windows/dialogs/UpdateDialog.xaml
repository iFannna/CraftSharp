<ui:FluentWindow x:Class="CraftSharp.Windows.Dialogs.UpdateDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title=""
                 Height="Auto" MinHeight="0" Width="420" MinWidth="320"
                 WindowStartupLocation="CenterOwner"
                 SizeToContent="Height"
                 MaxHeight="500"
                 Background="{DynamicResource ApplicationBackgroundBrush}"
                 ExtendsContentIntoTitleBar="True"
                 Foreground="{DynamicResource TextPrimaryBrush}"
                 ResizeMode="NoResize">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0"
                     Title="{Binding Title, RelativeSource={RelativeSource AncestorType=ui:FluentWindow}}"
                     ShowMinimize="False" ShowMaximize="False" ShowClose="True"
                     FontSize="{DynamicResource GlobalFontSize}" FontWeight="Medium"
                     Background="{DynamicResource ApplicationBackgroundBrush}"/>

        <ScrollViewer Grid.Row="1" MaxHeight="300" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="16">
                <TextBlock x:Name="NewVersionText" FontSize="{DynamicResource GlobalFontSizeSection}" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimaryBrush}"
                           Margin="0,0,0,12"/>
                <TextBlock x:Name="ChangelogText" TextWrapping="Wrap"
                           Foreground="{DynamicResource TextSecondaryBrush}"/>
            </StackPanel>
        </ScrollViewer>

        <Grid Grid.Row="2" Margin="16,0,16,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ui:Button Grid.Column="1" x:Name="SkipButton"
                       Content="{DynamicResource UpdateDialogSkip}"
                       Appearance="Secondary"
                       Margin="0,0,8,0"
                       Click="SkipButton_Click"/>
            <ui:Button Grid.Column="2" x:Name="DownloadButton"
                       Content="{DynamicResource UpdateDialogDownload}"
                       Appearance="Primary"
                       Click="DownloadButton_Click"/>
        </Grid>
    </Grid>
</ui:FluentWindow>
