<ui:FluentWindow x:Class="CraftSharp.Windows.Dialogs.MessageDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title=""
                 Height="Auto" MinHeight="0" Width="320" MinWidth="280"
                 WindowStartupLocation="CenterOwner"
                 SizeToContent="Height"
                 Background="{DynamicResource ApplicationBackgroundBrush}"
                 ExtendsContentIntoTitleBar="True"
                 Foreground="{DynamicResource TextPrimaryBrush}"
                 ResizeMode="NoResize">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="{Binding Title, RelativeSource={RelativeSource AncestorType=ui:FluentWindow}}"
                     ShowMinimize="False" ShowMaximize="False" ShowClose="True"
                     FontSize="{DynamicResource GlobalFontSize}" FontWeight="Medium"
                     Background="{DynamicResource ApplicationBackgroundBrush}"/>

        <StackPanel Grid.Row="1" Margin="16">
            <TextBlock x:Name="MessageText" TextWrapping="Wrap"
                       Foreground="{DynamicResource TextPrimaryBrush}"/>
        </StackPanel>

        <Grid Grid.Row="2" Margin="16,0,16,16">
            <ui:Button x:Name="OkButton"
                       Content="{DynamicResource MessageDialogOk}"
                       Appearance="Primary"
                       HorizontalAlignment="Right"
                       Click="OkButton_Click"/>
        </Grid>
    </Grid>
</ui:FluentWindow>
